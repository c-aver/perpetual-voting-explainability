{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-build",
			"label": "docker-build",
			"platform": "python",
			"dockerBuild": {
				"tag": "perpetualvotingexplainability:latest",
				"dockerfile": "${workspaceFolder}\\backend\\survey-server\\Dockerfile",
				"context": "${workspaceFolder}\\backend\\survey-server",
				"pull": true
			}
		},
		{
			"type": "docker-run",
			"label": "docker-run: debug",
			"dependsOn": [
				"docker-build"
			],
			"dockerRun": {
				"env": {
					"FLASK_APP": "backend\\survey-server\\src\\server.py"
				}
			},
			"python": {
				"args": [
					"run",
					"--no-debugger",
					"--no-reload",
					"--host",
					"0.0.0.0",
					"--port",
					"8080"
				],
				"module": "flask"
			}
		}
	]
}